name: CD - Deploy Development

on:
  workflow_run:
    workflows: ["Pipeline CI"]  # Nome do workflow do CI (deve bater exatamente)
    types:
      - completed
      
jobs:
  deploy:
    runs-on: [self-hosted, devsecops]
    environment: staging
    steps:
      - name: Login no Azure (AKS)
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Obter contexto do AKS
        uses: azure/aks-set-context@v3
        with:
          resource-group: ${{ secrets.AZURE_RESOURCE_GROUP }}
          cluster-name: ${{ secrets.AZURE_CLUSTER_NAME }}

      - name: Aplicar imagem no deployment
        run: |
          kubectl set image deployment/my-deployment \
            my-container=ghcr.io/${{ github.repository_owner }}/myapp:${{ inputs.image_tag }} \
            --namespace=staging

      - name: Verificar rollout
        run: |
          kubectl rollout status deployment/my-deployment --namespace=staging
